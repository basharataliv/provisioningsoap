package com.provisioning.gateway.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.provisioning.gateway.model.Chargeprocess;

public interface ChargeprocessRepository extends JpaRepository<Chargeprocess, Integer> {
}
